# 에코센스 발주서 - 배출 전류계 기준 전환

## 📅 수정 일시
2025-11-03

## 🔄 주요 변경 사항

### 1. 전류계 타입 설정 기준 변경
**변경 전**: 송풍+펌프 전류계 기준
**변경 후**: 배출 전류계 기준

**이유**: 대부분의 사업장에서 배출 전류계 수량이 더 많기 때문

### 2. 동적 수량 조정 기능 추가
**변경 전**: 16L/24L/36L 모두 수동 입력, 합계 제한만 검증
**변경 후**: 24L/36L 수정 시 16L 자동 계산 (전체 수량 유지)

### 3. 발주 금액 정보 추가
**추가 내용**: 소계, 부가세, 합계 금액 표시

## ✅ 수정된 파일

### 1. `app/admin/document-automation/components/PurchaseOrderModal.tsx`

#### A. 레이블 및 기준 변경

**변경 전**:
```typescript
<h3>송풍+펌프 전류계 굵기 설정</h3>
<p>송풍+펌프 전류계 수량: {fanPumpTotal}개</p>
<p>* 배출 전류계는 항상 16L로 표시됩니다.</p>
```

**변경 후**:
```typescript
<h3>전류계 타입 설정 (배출 전류계 기준)</h3>
<p>배출 전류계 수량: {dischargeCt}개
   (16L: {ct_16l}개, 24L: {ct_24l}개, 36L: {ct_36l}개)</p>
<p>* 송풍+펌프 전류계는 항상 16L로 표시됩니다.</p>
```

#### B. 동적 수량 조정 로직

**16L 입력 필드** - 자동 계산, 읽기 전용:
```typescript
<label>16L 수량 (자동 계산)</label>
<input
  type="number"
  value={ct_16l || 0}
  readOnly
  className="bg-gray-100 cursor-not-allowed"
/>
```

**24L 입력 필드** - 수정 시 16L 자동 조정:
```typescript
<input
  type="number"
  value={ct_24l || 0}
  onChange={(e) => {
    const dischargeCt = equipment?.discharge_ct || 0
    const ct24l = Math.max(0, Number(e.target.value))
    const ct36l = editedData.ct_36l || 0

    // 24L + 36L이 배출 전류계를 초과하지 않도록
    if (ct24l + ct36l <= dischargeCt) {
      const ct16l = dischargeCt - ct24l - ct36l  // 자동 계산
      setEditedData({
        ...editedData,
        ct_16l: ct16l,
        ct_24l: ct24l
      })
    }
  }}
/>
```

**36L 입력 필드** - 수정 시 16L 자동 조정:
```typescript
<input
  type="number"
  value={ct_36l || 0}
  onChange={(e) => {
    const dischargeCt = equipment?.discharge_ct || 0
    const ct24l = editedData.ct_24l || 0
    const ct36l = Math.max(0, Number(e.target.value))

    if (ct24l + ct36l <= dischargeCt) {
      const ct16l = dischargeCt - ct24l - ct36l  // 자동 계산
      setEditedData({
        ...editedData,
        ct_16l: ct16l,
        ct_36l: ct36l
      })
    }
  }}
/>
```

#### C. 기본값 설정 변경

**변경 전**:
```typescript
const fanPumpTotal = (equipment?.fan_ct || 0) + (equipment?.pump_ct || 0)
loadedData.ct_16l = fanPumpTotal
```

**변경 후**:
```typescript
const dischargeCt = equipment?.discharge_ct || 0
loadedData.ct_16l = dischargeCt
```

#### D. 발주 금액 정보 추가

```typescript
{/* 발주 금액 정보 */}
{editedData.item_details && editedData.item_details.length > 0 && (
  <div className="bg-white rounded-lg p-4 border border-gray-200">
    <h3 className="font-semibold text-gray-900 mb-3">발주 금액</h3>
    <div className="space-y-2">
      <div className="flex justify-between text-sm">
        <span className="text-gray-600">소계</span>
        <span className="font-medium">{editedData.subtotal?.toLocaleString() || 0}원</span>
      </div>
      <div className="flex justify-between text-sm">
        <span className="text-gray-600">부가세 (10%)</span>
        <span className="font-medium">{editedData.vat?.toLocaleString() || 0}원</span>
      </div>
      <div className="flex justify-between text-base pt-2 border-t border-gray-200">
        <span className="font-semibold text-gray-900">합계</span>
        <span className="font-bold text-blue-600">{editedData.grand_total?.toLocaleString() || 0}원</span>
      </div>
    </div>
  </div>
)}
```

### 2. `components/EcosensePurchaseOrderForm.tsx`

#### 송풍+펌프 전류계 표시
**변경 전**: ct_16l 값 사용
**변경 후**: fanPumpTotal 값 고정 사용

```typescript
// 변경 전
<td className="text-center">{data.ct_16l || fanPumpTotal}</td>

// 변경 후
<td className="text-center">{fanPumpTotal}</td>
```

#### 배출 전류계 표시
**변경 전**: 항상 16L 고정
**변경 후**: ct_16l/ct_24l/ct_36l 값 사용

```typescript
// 변경 전
<td className="text-center">{dischargeCt}</td>
<td className="text-center">0</td>
<td className="text-center">0</td>

// 변경 후
<td className="text-center">{data.ct_16l || dischargeCt}</td>
<td className="text-center">{data.ct_24l || 0}</td>
<td className="text-center">{data.ct_36l || 0}</td>
```

## 📊 동작 흐름

### 초기 로드 시
```
1. 사업장 데이터 조회
   - discharge_ct: 10개
   - fan_ct: 3개
   - pump_ct: 2개

2. 기본값 설정
   - ct_16l = 10 (배출 전류계 전체)
   - ct_24l = 0
   - ct_36l = 0

3. UI 표시
   - 배출 전류계 수량: 10개 (16L: 10개, 24L: 0개, 36L: 0개)
```

### 24L 수량 입력 시 (24L에 3개 입력)
```
1. 사용자 입력: 24L = 3

2. 검증
   - 24L(3) + 36L(0) = 3 <= 10 (배출 전류계) ✓

3. 자동 계산
   - ct_16l = 10 - 3 - 0 = 7
   - ct_24l = 3
   - ct_36l = 0

4. UI 업데이트
   - 배출 전류계 수량: 10개 (16L: 7개, 24L: 3개, 36L: 0개)
```

### 36L 수량 입력 시 (36L에 2개 입력)
```
1. 사용자 입력: 36L = 2

2. 검증
   - 24L(3) + 36L(2) = 5 <= 10 (배출 전류계) ✓

3. 자동 계산
   - ct_16l = 10 - 3 - 2 = 5
   - ct_24l = 3
   - ct_36l = 2

4. UI 업데이트
   - 배출 전류계 수량: 10개 (16L: 5개, 24L: 3개, 36L: 2개)
```

### 초과 입력 시도 (24L에 9개 입력 시도)
```
1. 사용자 입력: 24L = 9

2. 검증
   - 24L(9) + 36L(2) = 11 > 10 (배출 전류계) ✗

3. 입력 무시
   - 상태 업데이트 없음
   - 기존 값 유지 (24L: 3개)
```

## 📋 표시 예시

### 예시 1: 기본 상태 (배출 10개)
```
전류계 타입 설정 (배출 전류계 기준)
┌────────────────────┐
│ 16L: 10 (자동)     │
│ 24L:  0            │
│ 36L:  0            │
└────────────────────┘

발주서 표시:
┌──────────────┬────┬────┬────┐
│    구분       │16L │24L │36L │
├──────────────┼────┼────┼────┤
│송풍+펌프 전류계│ 5  │ 0  │ 0  │
│배출 전류계    │ 10 │ 0  │ 0  │
└──────────────┴────┴────┴────┘
```

### 예시 2: 타입 조정 (24L: 3, 36L: 2)
```
전류계 타입 설정 (배출 전류계 기준)
┌────────────────────┐
│ 16L:  5 (자동)     │
│ 24L:  3            │
│ 36L:  2            │
└────────────────────┘

발주서 표시:
┌──────────────┬────┬────┬────┐
│    구분       │16L │24L │36L │
├──────────────┼────┼────┼────┤
│송풍+펌프 전류계│ 5  │ 0  │ 0  │
│배출 전류계    │ 5  │ 3  │ 2  │
└──────────────┴────┴────┴────┘
```

### 예시 3: 발주 금액 표시
```
발주 금액
┌───────────────────────┐
│ 소계      5,000,000원 │
│ 부가세      500,000원 │
├───────────────────────┤
│ 합계      5,500,000원 │
└───────────────────────┘
```

## 🎯 사용자 경험 개선

### 1. 직관적인 조정
- **문제**: 이전에는 3개 필드를 모두 수동으로 맞춰야 했음
- **해결**: 24L/36L만 입력하면 16L 자동 계산

### 2. 오류 방지
- **문제**: 합계가 초과되는 입력 가능
- **해결**: 검증을 통해 초과 입력 자동 차단

### 3. 명확한 피드백
- **추가**: 실시간 수량 표시 (16L: X개, 24L: Y개, 36L: Z개)
- **추가**: 읽기 전용 필드에 "(자동 계산)" 표시

### 4. 금액 정보 제공
- **추가**: 소계, 부가세, 합계 금액 한눈에 확인 가능

## 🧪 테스트 시나리오

### 시나리오 1: 기본 동작
```
1. 사업장 선택 (배출 전류계 10개)
2. 발주서 생성 모달 열기
3. 전류계 타입 설정 확인
   - 16L: 10개 (자동), 24L: 0개, 36L: 0개
4. 미리보기 확인
   - 배출 전류계: 16L 10개
```

### 시나리오 2: 24L 입력
```
1. 24L에 3 입력
2. 자동 계산 확인
   - 16L: 7개 (자동), 24L: 3개, 36L: 0개
3. 미리보기 확인
   - 배출 전류계: 16L 7개, 24L 3개
```

### 시나리오 3: 36L 추가 입력
```
1. 36L에 2 입력
2. 자동 계산 확인
   - 16L: 5개 (자동), 24L: 3개, 36L: 2개
3. 미리보기 확인
   - 배출 전류계: 16L 5개, 24L 3개, 36L 2개
```

### 시나리오 4: 초과 입력 시도
```
1. 현재 상태: 24L 3개, 36L 2개
2. 24L에 9 입력 시도
   - 9 + 2 = 11 > 10 (배출 전류계)
3. 입력 무시 확인
   - 24L: 3개 유지
```

### 시나리오 5: 금액 표시
```
1. 발주서 생성 모달에서 "발주 금액" 섹션 확인
2. 소계, 부가세, 합계 표시 확인
3. 금액 형식 확인 (천 단위 구분)
```

## 🔍 검증 로직

### 입력 검증
```typescript
const dischargeCt = equipment?.discharge_ct || 0
const ct24l = Number(e.target.value)
const ct36l = editedData.ct_36l || 0

// 검증: 24L + 36L <= 배출 전류계
if (ct24l + ct36l <= dischargeCt) {
  // 자동 계산: 16L = 배출 전류계 - 24L - 36L
  const ct16l = dischargeCt - ct24l - ct36l

  // 상태 업데이트
  setEditedData({
    ...editedData,
    ct_16l: ct16l,
    ct_24l: ct24l
  })
}
// 검증 실패 시 상태 업데이트 없음 (기존 값 유지)
```

## ✅ 완료 체크리스트

- [x] 레이블 변경 (송풍+펌프 → 배출 전류계 기준)
- [x] 16L 자동 계산 로직 구현
- [x] 16L 입력 필드 읽기 전용 처리
- [x] 24L/36L 수정 시 16L 자동 조정
- [x] 검증 로직 구현 (24L + 36L <= 배출 전류계)
- [x] 실시간 수량 표시 추가
- [x] 기본값 설정 변경 (송풍+펌프 → 배출)
- [x] 발주서 폼 표시 로직 수정
- [x] 발주 금액 정보 추가
- [ ] 실제 데이터로 테스트

## 📝 관련 파일

- `app/admin/document-automation/components/PurchaseOrderModal.tsx` - 전류계 타입 설정 UI, 금액 표시
- `components/EcosensePurchaseOrderForm.tsx` - 전류계 굵기 표시
- `types/document-automation.ts` - ct_16l/ct_24l/ct_36l 타입 정의

## 🎉 주요 개선점

1. **사용자 편의성**: 24L/36L만 입력하면 16L 자동 계산
2. **오류 방지**: 초과 입력 자동 차단
3. **실시간 피드백**: 타입별 수량 즉시 확인 가능
4. **데이터 정확성**: 전체 수량 항상 유지
5. **금액 투명성**: 발주 금액 정보 명확하게 표시

## 🔮 향후 개선 사항

1. **프리셋 저장**: 자주 사용하는 타입 조합 저장
2. **타입별 단가**: 16L/24L/36L 각각 다른 단가 적용
3. **재고 연동**: 타입별 재고 확인 후 입력 가능 수량 표시
4. **통계 분석**: 타입별 사용 빈도 분석 및 추천
